import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

const CenteringBox = styled(Box)(() => ({
  display: "flex",
  alignItems: "center",
}));

export { CenteringBox };
